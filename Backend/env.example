# Backend/.env.example - Example environment configuration for the Flask backend

SECRET_KEY=your-secret-key-here

# For production/hosted database (Neon PostgreSQL):
DATABASE_URI=postgresql://username:password@host:port/database

# For local SQLite development (comment out above and uncomment below):
# DATABASE_URI=sqlite:///instance/sports_management.db

# For local PostgreSQL development:
# DATABASE_URI=postgresql://postgres:password@localhost:5432/sports_management
